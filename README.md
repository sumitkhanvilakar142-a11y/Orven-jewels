# ORVÉN JEWELS Website

Open `index.html` in a browser to preview the site.

## Before launch
1. Open `script.js` and replace `91XXXXXXXXXX` with the business WhatsApp number.
2. Replace the Instagram links with the real ORVÉN account.
3. Replace placeholder product images/content with actual product photos, names and prices.
4. Upload `index.html`, `style.css`, and `script.js` to your hosting provider.

No build system or framework is required.
